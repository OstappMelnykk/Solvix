// Production build (ng build --configuration production).
// apiBaseUrl empty - assumes the API is served from the same origin as the
// app; set this once a real production deployment target exists.
export const environment = {
  production: true,
  apiBaseUrl: ''
};